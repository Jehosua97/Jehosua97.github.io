# Optional. Add a secure HTTPS endpoint from your form provider (for example,
# a Formspree form endpoint). When left blank, the form uses a transparent
# mailto fallback and does not claim that a submission was delivered.
VITE_CONTACT_FORM_ENDPOINT=
